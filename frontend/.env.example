# Leave empty for local Docker development so requests use the Vite proxy.
# Set this to the backend URL only when connecting directly to an API that
# has CORS enabled (for example: http://localhost:3000).
VITE_API_BASE_URL=